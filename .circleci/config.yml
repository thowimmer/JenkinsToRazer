version: 2.1
jobs:
  build_windows_release_executable:
    machine:
      image: ubuntu-1604:201903-01
    steps:
       - checkout
       - run:
           command: |
               cd source/JenkinsToRazer
               ./gradlew_win linkReleaseExecutableWindows
               cp -r build/bin/windows/releaseExecutable/ /tmp/artifacts
       - store_artifacts:
           path: /tmp/artifacts
           
workflows:
  version: 2
  build_and_upload_release_artifacts:
    jobs:
      - build_windows_release_executable:
          filters:
            branches:
              only:
                - develop
